{
  "name": "hareshkarnan.github.io-tests",
  "version": "1.0.0",
  "description": "E2E and regression tests for Haresh Karnan's academic website",
  "main": "index.html",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:debug": "npx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/node": "^20.11.24"
  }
}
